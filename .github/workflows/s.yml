name: S
on:
  workflow_dispatch:
    inputs:
      file_path:
        description: path
        required: true
        default: photos/x.jpg
  push:
    branches: [test-trigger]
jobs:
  j:
    runs-on: ubuntu-latest
    steps:
      - name: a
        env:
          FILE_PATH: ${{ github.event.inputs.file_path || 'PHOTO' }}
        run: |
          if [[ "${{ github.event_name }}" != "workflow_dispatch" ]]; then echo "skip (push event)"; exit 0; fi
          echo "ok ${FILE_PATH}"
