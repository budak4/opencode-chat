name: T
on:
  workflow_dispatch:
    inputs:
      file_path:
        description: path
        required: true
        default: photos/file_4.jpg
  push:
    branches: [test-trigger]
permissions:
  contents: read
jobs:
  x:
    runs-on: ubuntu-latest
    steps:
      - run: echo "ok"
